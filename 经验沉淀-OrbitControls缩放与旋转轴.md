# 经验沉淀：OrbitControls「鼠标点缩放」与「旋转轴固定」如何兼顾

## 背景
在 `three` / `three-stdlib` 的 `OrbitControls` 里：
- **旋转**是绕 `controls.target` 做轨道旋转。
- `zoomToCursor=true` 会在缩放后**重算并修改 `controls.target`**，以实现“缩放时鼠标指向点尽量保持在屏幕位置”。

因此出现典型冲突：
- 你想要 **缩放跟随鼠标/触摸点**（便于放大树底部）
- 同时又想要 **拖拽旋转永远绕树干中心**（树不偏移）

如果直接用 `zoomToCursor`：
- 缩放体验好，但 `target` 会漂移，旋转轴会跟着漂 → 树看起来“绕偏了”。

## 两种方案（对比）
### 方案 A：开启 `zoomToCursor`
- **优点**：缩放中心最准（严格意义的“朝鼠标点缩放”）。
- **缺点**：必然改 `target`，如果你要“旋转轴固定树干中心”，就必须额外写一套“旋转时锁轴/重映射 target”的逻辑，复杂且容易抖动/跳变。

### 方案 B：锁定 `target`（树干轴）+ 自定义缩放
- **核心思路**：永远把 `controls.target` 锁定在树干中心；缩放时不让 OrbitControls 改 target，而是 **让相机沿鼠标/触摸点射线方向推进/拉远**。
- **优点**：旋转轴稳定，树不会偏移；缩放会“朝你指向的位置”走，能解决“放大后树下半部分永远看不到”的痛点。
- **缺点**：不保证“屏幕上的那个点完全不动”（不如 `zoomToCursor` 严格）。

本项目选择 **方案 B**（稳定、风险低、改动小）。

## 落地要点（踩坑清单）
### 1) 禁用 OrbitControls 默认缩放
- 设置 `enableZoom={false}`，否则它会吃掉滚轮/双指事件并应用自己的 dolly 逻辑。

### 2) 监听画布滚轮/双指缩放，必须 `passive:false`
- 滚轮/触摸事件要 `preventDefault()`，否则页面会跟着滚。
- 监听时用 `{ passive: false }` 才能生效。

### 3) 缩放数学：沿射线移动相机，同时约束相机到树中心距离
将相机位置记作 \(C\)，树中心为 \(P\)，射线单位方向为 \(d\)：
- 目标是让新距离 \(|C' - P|\) 变为 `nextDist`
- 同时令 \(C' = C + d \cdot m\)

解：
\[
|(C + d m) - P| = nextDist
\]
得到一元二次方程，可求出合适的 \(m\)（通常取较小根，推进/拉远更自然）。

### 4) 任何交互（滚轮/双指）都要切回 MANUAL
如果用户在预设视角下缩放，但你不切回 `MANUAL`，`useFrame` 的“预设视角 lerp”会把相机又拉回去，体验像“缩放失效”。

### 5) 树中心点要选对（与场景层级位移一致）
本项目里树整体 `group` 在 `Experience.tsx` 里是 `position={[0, -2, 0]}`，所以树干旋转轴中心建议设为 `(0, -2, 0)`，否则“看不全/旋转手感怪”会更明显。

## 额外：仰视（BOTTOM）预设为什么看不全
树高度约 14～15（`cylinderGeometry` 高 14 + 叶子/装饰略超出），如果仰视视角距离不够远或 target 指向偏上，会导致：
- 画面垂直 FOV 不够容纳整棵树
- 或者相机看向太高的位置，树底永远在画面外

实践：把 BOTTOM 的相机 **拉更远**、target 指向树干中部，并确保与树中心点一致。


